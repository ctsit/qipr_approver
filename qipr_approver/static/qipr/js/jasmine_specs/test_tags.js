describe('Tagbox tests', function () {
    var mockument = document.createDocumentFragment();

    it('should define window.modules.TaggerFactory and window.Tagger', function () {
        expect(window.modules.TaggerFactory).toBeDefined();
        expect(window.Tagger).toBeDefined();
    });

    describe('', function () {
        beforeEach(function () {
        });
    });
});
